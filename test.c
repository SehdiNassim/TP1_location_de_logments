// Fichier de test, ne sert a rien
// Created by X360 on 26/03/2018.
//
/*#include "LOG_LLC_BIBLIO.h"

FILE * f;

ListeLogement * log;
ListeLocation * loct;
ListeLocataire * loc;

int main() {
    initLogement(f, &log);
    initLocataire(f, &loc);
    initLocation(f, &loct, log);

    ListeLocation *p = loct;
    while (p != NULL) {
        printf("%.0fDZD %ld %ld", p->fiche.loyer, p->fiche.dateDeb, p->fiche.dateFin);
        p = suivLocation(p);
    } 
}*/